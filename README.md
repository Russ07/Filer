# 📁 Filer

A modern React + Tailwind CSS Progressive Web App (PWA) for uploading, previewing, and editing Excel, Word, and PDF files directly in the browser — fast, responsive, and offline-ready.

## 🚀 Features
- Upload and preview `.xlsx`, `.docx`, and `.pdf` files
- Responsive UI built with Tailwind CSS
- Offline support via PWA architecture
- Modular React components for easy extension
- Optional Node.js backend for advanced processing

## 🛠️ Tech Stack
- **Frontend:** React (Create React App), Tailwind CSS, FileReader API, PDF.js, SheetJS
- **Backend (optional):** Node.js, Express

## 📦 Installation

### Clone the repo

bash
`git clone https://github.com/your-username/filer
cd filer`

Install frontend dependencies
`cd client
npm install
npm start`

If using the backend
`cd server
npm install
npm run dev`



